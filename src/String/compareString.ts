function compareString(firstString: string, secondString: string) {

  let equal = firstString.toUpperCase() === secondString.toUpperCase();

  return equal;

}

function compareString(firstString: string, secondString: string) {

  let equal = firstString.toLowerCase() === secondString.toLowerCase();

  return equal;

}